/**
 * Exercise 01 — Greet
 *
 * Goal: return a greeting string.
 * Example: greet("Alex") → "Hello, Alex"
 *
 * Tip: use + to join strings, or a template like `Hello, ${name}`
 */

function greet(name) {
  // your code here
}

module.exports = { greet };
