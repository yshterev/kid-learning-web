/**
 * Exercise 04 — First item
 *
 * Goal: return the first item in an array.
 * Example: firstItem(["cat", "dog", "bird"]) → "cat"
 *
 * Tip: arrays start at index 0 → list[0]
 */

function firstItem(list) {
  // your code here
}

module.exports = { firstItem };
