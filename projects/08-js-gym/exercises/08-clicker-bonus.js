/**
 * Exercise 08 — Clicker bonus
 *
 * Goal: every 10 clicks gives a bonus.
 * Return the bonus points for a given click count.
 *
 * Rules:
 * - clicks 0–9  → 0 bonus
 * - clicks 10–19 → 1 bonus
 * - clicks 20–29 → 2 bonus
 * - and so on (bonus = how many full groups of 10)
 *
 * Example: clickerBonus(0)  → 0
 * Example: clickerBonus(10) → 1
 * Example: clickerBonus(25) → 2
 *
 * Tip: Math.floor(clicks / 10)
 */

function clickerBonus(clicks) {
  // your code here
}

module.exports = { clickerBonus };
