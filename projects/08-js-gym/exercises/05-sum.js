/**
 * Exercise 05 — Sum
 *
 * Goal: add up every number in an array and return the total.
 * Example: sum([1, 2, 3]) → 6
 *
 * Tip: use a loop (for...of) or a counter variable
 */

function sum(numbers) {
  // your code here
}

module.exports = { sum };
